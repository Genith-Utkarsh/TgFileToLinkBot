# ── Telegram ──────────────────────────────────────────────────────
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
ALLOWED_USER_ID=0          # Your numeric Telegram user ID (0 = allow all)
# TELEGRAM_API_URL=http://localhost:8081  # Set to a Local Bot API server to allow files > 20MB

# ── Streaming API ────────────────────────────────────────────────
API_SECRET_TOKEN=changeme  # Shared secret for stream URLs
BASE_URL=http://localhost:8000  # Public-facing URL root

# ── Server ───────────────────────────────────────────────────────
# HOST=0.0.0.0             # Bind address
# PORT=8000                # Server port
# CHUNK_SIZE=524288         # Proxy chunk size in bytes (default 512 KB)
# MAX_CONNECTIONS=100       # httpx connection pool size
# LOG_LEVEL=INFO            # Logging level (DEBUG, INFO, WARNING, ERROR)
